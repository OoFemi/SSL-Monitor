version: "3.9"

services:
  uptime-monitor:
    build: .
    container_name: uptime-monitor
    ports:
      - "7000:7000"
    volumes:
      - ./config.properties:/app/config.properties
    restart: unless-stopped
